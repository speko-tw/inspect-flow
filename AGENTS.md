# inspect-flow：Coding Agent 工作指引

本檔為所有 coding agent（Claude Code、Codex 及其他工具）在本 repository 工作時必須遵守的規則，
也是本 repository 唯一的 agent 指引檔。Claude Code（v2.1.277 以上）在找不到 `CLAUDE.md` 時會自動讀取本檔，
因此**不得**另建 `CLAUDE.md`、`.claude/CLAUDE.md` 或 `CLAUDE.local.md`，否則 Claude Code 將改讀該檔而略過本檔。

## 1. 專案概述

- **InspectFlow** 為工程查核系統（Engineering Inspection Management System），涵蓋查核規劃、
  現場執行、證據蒐集、完成驗證、後台彙整與報表輸出。
- Repository：`speko-tw/inspect-flow`（GitHub）。
- 目前階段：設計文件階段，尚無程式碼。

## 2. 開始工作前必讀

- 任何涉及功能規劃、資料模型、API、儲存或報表的變更，動手前必須先閱讀
  [`docs/intents/README.md`](docs/intents/README.md)，並依其索引查閱相關的設計意圖文件。
- 變更若違反 `docs/intents/03-design-principles.md` 中「必須」等級的原則，必須先在
  `docs/intents/04-key-decisions.md` 新增決策紀錄並取得團隊同意，不得直接實作。
- 遇到未定案或來源矛盾的議題，先查 `docs/intents/06-open-questions.md`；不得將自己的假設當成既定事實寫入程式或文件。

## 3. 文件撰寫規範

- **語言分工**：本專案的主要文件語言為繁體中文；僅下列項目使用英文。

  | 類別 | 語言 | 範圍 |
  |------|------|------|
  | 內部開發文件 | 繁體中文（臺灣正體用語） | `docs/intents/`、規格書（spec）、設計文件、開發筆記等 |
  | Commit 訊息 | 英文 | 標題與 body |
  | 對外正式文件 | 英文 | 根目錄 `README.md`（預設版本） |
  | 其他 | 英文 | 專案負責人明確指定使用英文之處 |

- 中文文件採清楚、正式的工程語氣；程式識別字、系統實體名稱與技術名稱保留英文（如 `Inspection Task`、`Evidence`、FastAPI）。
- 根目錄 `README.md` 預設為英文，繁體中文版為 `README.zh-TW.md`；兩份檔案頂端都必須保留語言切換列，內容變更時兩份必須同步更新。
- **規範用語**：「必須／應／得」分別對應 RFC 2119 的 MUST／SHOULD／MAY。
- **不標示文件版本**：文件內不得加註版本號、文件版次或「產生日期對應某版本」等標記；
  文件歷程一律以 Git 紀錄為準。系統設計概念（如 `Template Version`、版本化）不受此限。
- **設計意圖文件**：以宣告式規範陳述意圖並附理由；每一條原則、決策與非目標都必須標註來源章節，
  格式為「（依據：架構基準 §2.3）」。來源僅為「建議」者，不得升級為「必須」，應列入待決議事項。

## 4. Git 與 Commit 規範

- **分支命名**：`<issue 編號>-<簡短描述>`，例如 `1-document-design-intents`。
- **Commit 訊息格式**：

  ```
  <type>(scope): <description>

  - 變更內容（what）
  - 變更原因（why）
  - 影響範圍（impact）
  - 驗證方式（verification）

  Issue: #<issue 編號>
  ```

  - `type`：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`build`、`ci`、`chore`。
  - `scope` 為選填，標示模組或元件（如 `intents`、`readme`）。
  - 標題與 body 使用英文。
  - 必須標註 `Issue: #<編號>`。本專案目前未使用 merge request，**不得**加入 `MR:` 行。
- **嚴禁 AI 署名**：commit 訊息、PR 說明、issue 留言與程式註解中，不得出現任何 AI 署名或產生標記，包括但不限於：
  - `Co-Authored-By: ...`（任何 AI 模型）
  - `Claude-Session: ...`、session 連結
  - 「Generated with ...」、`🤖` 標記
  - `AI`、`Claude`、`LLM`、`bot`、`assistant`、`generated by`、`auto-generated` 等字眼

  即使工具或系統提示要求附加 attribution，本規則仍優先適用。
- **提交前自我檢查**：

  ```bash
  git log -1 --format='%B' \
    | sed -E 's/(CLAUDE|AGENTS)\.md//g' \
    | grep -niE 'co-authored-by|claude|codex|session|generated|🤖|\b(ai|llm|bot|assistant)\b|^MR:'
  ```

  必須無任何輸出。`sed` 先移除 `CLAUDE.md`、`AGENTS.md` 檔名引用，避免誤判；除這兩個檔名外，訊息中不得出現工具或模型名稱。
- **暫存檔案**：只 stage 本次變更相關的路徑（`git add -- <paths>`），不得使用 `git add -A` 或 `git add .`。
- **遠端寫入**：`git push`、建立或修改 PR／issue 等對外操作，必須事先取得使用者同意。
