# 環境變數範本。
#
# 用途：作為未來新增環境變數時的範本；複製成 .env 使用，
# .env 不得提交到版本控制。

# 後端資料庫連線目標（database-foundation DBF-R05）。
# 值是 SQLAlchemy 連線字串，決定後端連到 SQLite 檔案還是
# PostgreSQL，不需改程式碼即可切換。
#
# 未設定，或設成空字串，都會使用預設值：backend 目錄下的
# data/inspectflow.db（SQLite 檔案），路徑由程式依自身檔案位置
# 推算為絕對路徑，與執行指令時所在的工作目錄無關。
#
# 範例（不要在這個檔案填真實連線字串，只留範例格式）：
#   SQLite（自訂路徑）：
#     INSPECTFLOW_DATABASE_URL=sqlite:////absolute/path/to/db.sqlite3
#   PostgreSQL：
#     INSPECTFLOW_DATABASE_URL=postgresql+psycopg://user:password@host:5432/inspectflow
# INSPECTFLOW_DATABASE_URL=

# `make check` 的 PostgreSQL 相容性檢查專用資料庫（DBF-R10）。
#
# 用途：`make check` 內的 check-postgres 用這個連線字串跑一次
# `alembic upgrade head` 與 backend/tests/db/ 的測試（只適用 SQLite
# 的測試會略過），確保 migration 與資料庫相關行為在 PostgreSQL
# 上也正確，不只在 SQLite 上驗證過。
#
# 未設定，或設成空字串，都會直接略過 check-postgres（印出一行
# SKIPPED 訊息），不影響其餘檢查。
#
# 這個資料庫在每次檢查開始時都會被清空（重置整個 public
# schema），因此務必指向一個專用的測試資料庫，不可以指向任何
# 有實際資料的資料庫。
#
# 與 INSPECTFLOW_DATABASE_URL 的關係：check-postgres 執行 alembic
# 時，Makefile 只把這個值「臨時」設成子行程的 INSPECTFLOW_DATABASE_URL
# 環境變數（不影響你 shell 或 .env 裡原本的設定），讓 alembic CLI
# 與 env.py 讀到同一個連線變數。兩者不要指向同一個有資料的資料庫。
#
# 範例（不要在這個檔案填真實連線字串，只留範例格式）：
#   INSPECTFLOW_TEST_POSTGRES_URL=postgresql+psycopg://user:password@localhost:5432/inspectflow_test
# INSPECTFLOW_TEST_POSTGRES_URL=
